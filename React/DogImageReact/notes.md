User X
1) Click the button
2) trigger event with clicking
3) http request => utlize an endpoint
4) Image will change



useState()
fetch() to make calls


fetch() => utilize endpoint
.then() => get a response, if good, parse the data, else error
.then() => do something with that parsedData
.catch() => error handling

axios()
.then()  => combines both thens from fetch
.catch()

Consuming an API
1) endpoints
2) verbs - GET, POST
3) what kind of data = JSON or XML
4) What that data looks like
5) How much are we getting back
